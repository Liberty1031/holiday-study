2026.2.2变量与常量
1.主函数前为“全局变量”，后为“局部变量”。
2.在局部变量中，不可将一个变量重复定义，例如：int a=1,int a=2,这是错误的。
3.计算两个整数的和
  1）.将两个变量初始化，例:int num1=0,int num2=0.
  2）.include<stdio.h>
      int main(){
      int num1=0
      int nu}